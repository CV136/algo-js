
function calcSurface(a, b) {
    return a*b;
}

let a = prompt("Enter the length of the rectangle");
let b = prompt("Enter the width of the rectangle");
let surface = calcSurface(a, b);
console.log(surface)

